name: CI

# Continuous integration for bioresearch-ai.
#
# Three parallel jobs run on every push to master and on
# every pull request targeting master:
#
#   1. backend-tests: runs the pytest suite (FSM, citation
#      validator, orchestrator, routes). Uses the MINIMAL
#      requirements set so the test environment matches
#      what gets installed in the production Docker image
#      -- if a test passes locally but fails on minimal
#      deps, that's a real regression.
#
#   2. frontend-tests: runs npm test (vitest). Tests
#      React components, hooks, and TypeScript models.
#
#   3. docker-build: builds the production :minimal
#      Docker image, starts a container from it, and
#      runs scripts/verify-ci.sh against it. The verify
#      script hits every /admin/* endpoint, exercises a
#      real workspace + DOI fetch, and verifies the
#      force-refresh + clear-cache admin endpoints work.
#      Catches Dockerfile regressions AND admin endpoint
#      regressions that unit tests can't.
#
# Total wall-clock target: under 6 minutes on a fresh
# runner. The docker-build job is the slowest at ~3 min
# for the cold image cache.
#
# We do NOT run the live /admin/* smoke check in CI
# because:
#
#   - It requires real LLM credentials
#   - It needs network access to CrossRef/OpenAlex/etc.
#   - It's already covered by the manual live-verify
#     pattern documented in the project root
#
# The manual live-verify is the right tool for
# end-to-end contract checks. CI is for fast, hermetic
# regressions.

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

# Cancel any in-progress runs for the same branch on
# a new push -- saves runner minutes when someone
# force-pushes or iterates quickly.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ------------------------------------------------------------------
  # 1. Backend tests
  # ------------------------------------------------------------------
  backend-tests:
    name: Backend tests (Python 3.12)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements/minimal-requirements.txt
            requirements.txt

      - name: Create virtualenv
        run: |
          python -m venv .venv
          . .venv/bin/activate
          python -m pip install --upgrade pip

      - name: Install minimal backend dependencies
        run: |
          . .venv/bin/activate
          # The CI runs against the MINIMAL set so the
          # tests have to work with what the Docker
          # image actually ships with. This catches
          # "test passes locally with full dev deps but
          # breaks in the slim image" regressions -- a
          # class of bug we have hit before.
          pip install -r requirements/minimal-requirements.txt
          # Plus the test-only deps that aren't in
          # minimal-requirements.txt.
          pip install pytest pytest-asyncio
          # fakeredis>=2.20: in-process Redis emulator
          # used by tests/unit/test_cache_protocol.py to
          # exercise the RedisCache implementation without
          # needing a running Redis server. Lower bound
          # only -- the fakeredis 2.x API is stable.
          pip install 'fakeredis>=2.20'

      - name: Run pytest
        env:
          # Set the env vars our settings module requires
          # at instantiation time. CI doesn't have a
          # .env file so we provide minimal stubs.
          # All values are quoted as strings to avoid
          # YAML parsing quirks around colons in URLs.
          APP_ENVIRONMENT: "test"
          DEFAULT_LLM_PROVIDER: "openai"
          OPENAI_API_KEY: "ci-stub-key"
          OPENAI_BASE_URL: "http://localhost:9"
          PUBMED_EMAIL: "ci@example.com"
          DATABASE_URL: "sqlite:///:memory:"
          ABSTRACT_ENRICHER_ENABLED: "false"
          LLM_ABSTRACT_EXTRACTION_ENABLED: "false"
          LOG_LEVEL: "WARNING"
        run: |
          . .venv/bin/activate
          PYTHONPATH=. python -m pytest tests/ -q --tb=short

  # ------------------------------------------------------------------
  # 2. Frontend tests
  # ------------------------------------------------------------------
  frontend-tests:
    name: Frontend tests (Node 20)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node 20
        # Pin to the version used in production
        # (frontend/Dockerfile uses node:20-bookworm-slim).
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        working-directory: frontend
        # npm ci is the deterministic equivalent of npm
        # install -- it uses package-lock.json exactly
        # and never modifies it. Required for reproducible
        # CI builds.
        run: npm ci

      - name: Run vitest
        working-directory: frontend
        run: npm test

      - name: Type-check + build
        working-directory: frontend
        # A separate build step catches type errors that
        # vitest might tolerate (vitest uses esbuild which
        # is more permissive than tsc).
        run: npm run build

  # ------------------------------------------------------------------
  # 3. Docker build
  # ------------------------------------------------------------------
  docker-build:
    name: Docker build (:minimal)
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Buildx
        # BuildKit is required for the multi-stage
        # Dockerfile (--from=frontend, --chown=app:app).
        uses: docker/setup-buildx-action@v3

      - name: Build :minimal image
        # We don't push from CI -- the image is just
        # validated, not published. The smoke-test step
        # below starts a container from the local image
        # to confirm the entrypoint actually boots.
        run: |
          docker build \
            --target backend-minimal \
            -t bioresearch-ai:ci-minimal \
            .

      - name: Install verify.sh prerequisites
        # verify-ci.sh needs bash, curl, jq. bash + curl
        # come with the GitHub-hosted runner image; jq
        # does not.
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends jq

      - name: Smoke-test container boot + admin battery
        # Bring up the container, then run scripts/verify-ci.sh
        # against it. verify-ci.sh hits every /admin/*
        # endpoint plus exercises a real workspace +
        # DOI fetch + force-refresh + cache clear, and
        # exits non-zero on any failure.
        #
        # This catches regressions that the simple
        # ``curl /health`` check would miss: broken
        # admin endpoints, schema changes in the FSM
        # counts endpoint, force-refresh endpoint bugs,
        # etc.
        #
        # The verify-ci.sh container check is opt-in via
        # VERIFY_CI=1 -- if a future change makes the
        # admin endpoints depend on something the smoke
        # container can't provide (e.g. real LLM creds),
        # setting VERIFY_CI=0 in the workflow disables
        # just the smoke step without removing the rest
        # of the job.
        env:
          VERIFY_CI: "1"
        run: |
          docker run -d \
            --name bioresearch-ai-ci-smoke \
            -p 8001:8000 \
            -e APP_ENVIRONMENT=test \
            -e DEFAULT_LLM_PROVIDER=openai \
            -e OPENAI_API_KEY=ci-stub-key \
            -e OPENAI_BASE_URL=http://localhost:9 \
            -e PUBMED_EMAIL=ci@example.com \
            -e "DATABASE_URL=sqlite:////tmp/ci-smoke.db" \
            -e ABSTRACT_ENRICHER_ENABLED=true \
            -e LLM_ABSTRACT_EXTRACTION_ENABLED=false \
            -e LOG_LEVEL=WARNING \
            bioresearch-ai:ci-minimal
          # Give the container up to 30 seconds to boot.
          for i in $(seq 1 30); do
            sleep 1
            if curl -fs http://localhost:8001/health \
              | grep -q '"status":"healthy"'; then
              echo "container healthy after ${i}s"
              break
            fi
            if [ "$i" -eq 30 ]; then
              echo "container failed to boot in 30s"
              docker logs bioresearch-ai-ci-smoke
              exit 1
            fi
          done
          # Now run the smoke battery. Set BACKEND_PORT=8001
          # so verify-ci.sh hits the smoke container
          # rather than localhost:8000.
          if [ "${VERIFY_CI}" = "1" ]; then
            BACKEND_PORT=8001 NO_COLOR=1 ./scripts/verify-ci.sh
          fi

      - name: Tear down smoke-test container
        if: always()
        run: |
          docker stop bioresearch-ai-ci-smoke || true
          docker rm -f bioresearch-ai-ci-smoke || true

  # ------------------------------------------------------------------
  # 4. Real-Redis integration tests
  # ------------------------------------------------------------------
  # Exercises the RedisCache implementation against a real
  # ``redis-server`` process, not fakeredis. Catches behavioral
  # drift that the unit tests miss: pipeline command
  # ordering, atomic INCR semantics, and the
  # misconfigured-URL error path (the unit tests use
  # fakeredis which never raises ConnectionError).
  #
  # The tests are opt-in via the REDIS_URL env var: without
  # it, they skip cleanly. The ``services:`` block below
  # spins up ``redis:7-alpine`` as a sidecar and exposes
  # REDIS_URL to the test runner.
  redis-integration:
    name: Real-Redis integration tests
    runs-on: ubuntu-latest
    timeout-minutes: 5
    services:
      redis:
        image: redis:7-alpine
        # The tests use a dedicated key prefix
        # (``bioresearch-test:<uuid>:``) per-test, so the
        # Redis instance can be shared across runs without
        # collisions. No health-check options needed beyond
        # the default -- redis:7-alpine's HEALTHCHECK
        # directive (``redis-cli ping``) is sufficient.
        ports:
          - 6379:6379
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements/minimal-requirements.txt
            requirements.txt

      - name: Create virtualenv + install deps
        run: |
          python -m venv .venv
          . .venv/bin/activate
          python -m pip install --upgrade pip
          # Install the same set as backend-tests so the
          # RedisCache module imports cleanly. We don't
          # need fakeredis here (we're testing against a
          # real server).
          pip install -r requirements/minimal-requirements.txt
          pip install pytest pytest-asyncio

      - name: Wait for Redis to be ready
        # The ``services:`` block starts the Redis
        # container in parallel with this job. On
        # GitHub-hosted runners, service containers are
        # exposed on the runner host via the ``host.docker.internal``
        # DNS name (the standard Docker-on-Linux pattern
        # for "the host running Docker"), with the mapped
        # ports forwarding through. ``localhost`` is the
        # runner host -- service container ports are not
        # directly bound to ``localhost`` on hosted runners.
        # ``host.docker.internal`` is the only host that
        # reliably resolves to the runner host from the
        # runner's own shell.
        #
        # The reliable wait is therefore:
        #   1. docker ps to find the Redis container
        #   2. docker exec into it to call redis-cli
        # This works regardless of network plumbing.
        run: |
          for i in $(seq 1 30); do
            CID=$(docker ps --filter "ancestor=redis:7-alpine" -q 2>/dev/null | head -n1)
            if [ -n "$CID" ]; then
              pong=$(docker exec "$CID" sh -c 'redis-cli ping' 2>/dev/null || true)
              if echo "$pong" | grep -q PONG; then
                echo "redis ready after ${i}s (container $CID)"
                break
              fi
            fi
            if [ "$i" -eq 30 ]; then
              echo "redis failed to come up in 30s"
              docker ps --filter "ancestor=redis:7-alpine" || true
              exit 1
            fi
            sleep 1
          done

      - name: Run Redis integration tests
        env:
          # On GitHub-hosted runners, service containers
          # are reached via the runner host's
          # ``host.docker.internal`` DNS name (the
          # standard Docker-on-Linux pattern). The tests
          # check this env var; if unset, they skip
          # cleanly (see
          # tests/integration/test_cache_redis_integration.py).
          REDIS_URL: "redis://host.docker.internal:6379/0"
          # Minimal stub env vars to satisfy
          # ``app.config.pubmed.PubMedSettings`` and
          # friends -- the integration test doesn't talk
          # to PubMed or any LLM, but settings are
          # instantiated at import time.
          APP_ENVIRONMENT: "test"
          DEFAULT_LLM_PROVIDER: "openai"
          OPENAI_API_KEY: "ci-stub-key"
          OPENAI_BASE_URL: "http://localhost:9"
          PUBMED_EMAIL: "ci@example.com"
          DATABASE_URL: "sqlite:///:memory:"
          ABSTRACT_ENRICHER_ENABLED: "false"
          LLM_ABSTRACT_EXTRACTION_ENABLED: "false"
          LOG_LEVEL: "WARNING"
        run: |
          . .venv/bin/activate
          PYTHONPATH=. python -m pytest \
            tests/integration/test_cache_redis_integration.py \
            -v --tb=short

